{
  "name": "Cloud Function",
  "version": "1.0.0",
  "description": "Google Cloud Function",
  "main": "ts-built/index.js",
  "license": "MIT",
  "scripts": {
    "gcp-build": "npm run build",
    "build": "tsc",
    "start": "yarn run build && npx @google-cloud/functions-framework --target=hello"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.2",
    "typescript": "^4.7.4"
  }
}
